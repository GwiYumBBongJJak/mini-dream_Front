import { Button, Input, TextArea } from "../../common";

const BoardEditPage = () => {
	return (
		<>
			<Button>뒤로가기</Button>
			<form>
				<Input />
				<Button>등록</Button>
				<TextArea />
			</form>
		</>
	);
};

export default BoardEditPage;
