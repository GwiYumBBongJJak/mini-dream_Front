import { Button, Input, TextArea } from "../../common";

const BoardEditPage = () => {
	return (
		<>
			<Button>뒤로가기</Button>
			<Input />
			<Button>등록</Button>
			<TextArea />
		</>
	);
};

export default BoardEditPage;
